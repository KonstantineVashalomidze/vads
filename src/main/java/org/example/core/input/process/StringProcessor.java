package org.example.core.input.process;

import org.example.core.input.model.Data;

public class StringProcessor
    implements DataProcessor
{
    @Override
    public byte[] process(Data data) {
        return new byte[0];
    }
}
